# Projeto - Portal de Tarefas

Criação de um portal aonde você pode controlar suas tarefas e sua agenda do dia.

## Setup

Make sure to install the dependencies:

```bash

npm install
npm run dev

## Development Server

Start the development server on `http://localhost:3000`:
